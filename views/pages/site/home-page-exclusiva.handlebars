<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <title>Página exclusiva - {{produto.descricao}}</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <link rel="stylesheet"
        href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
          <link
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous"
        />
    <!-- Font Awesome (opcional; pode ficar antes ou depois do Bootstrap) -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      referrerpolicy="no-referrer"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Seu CSS por último para sobrescrever o Bootstrap -->
    <link rel="stylesheet" href="/css/home.css">
    {{!-- <link rel="stylesheet" href="/public/css/home-layout.css"> --}}
<style>
    /* HEADER AZUL NO TOPO (100%) */
    #headerLojaExclusiva {
      width: 100%;
      background: #0069a8;
      color: #fff;
      padding: 10px 10px;
      font-size: 1.4rem;
      font-weight: 600;
      text-align: center;
      margin-bottom: 10px;
    }

    #headerLojaExclusiva a {
      color: #fff;
      text-decoration: none;
    }

    #headerLojaExclusiva a:hover {
      text-decoration: underline;
    }
</style>
<style>
    /* ÁREA PRINCIPAL */
    .page-shell{
      background: #a2a1a1;
      border-left: 2px solid #9a9797;
      padding: 0 16px;         /* respiro lateral */
    }

    .page-wrapper aside{min-width: 0;}

    .page-wrapper main{
      min-width: 0;        /* ESSENCIAL: permite o conteúdo encolher sem quebrar */
    }

    .cards-row{
        display: grid !important;
        grid-template-columns: repeat(4, minmax(220px, 1fr));
        gap: 16px;
        margin: 0 !important;   /* mata margin negativa do .row */
        justify-content: center; /* <- resolve “jogado pra esquerda” */
    }

    /* remove o “col-” interferindo no grid */
    .cards-row > [class*="col-"]{
      padding: 0 !important;
      max-width: none !important;
      flex: initial !important;
    }

    /* CAIXA CENTRAL (simetria do conteúdo) */
    .page-inner{
      max-width: 1320px;                 /* ajuste: 1200/1280/1360 */
      margin: 0 auto;                    /* centraliza a caixa */
      background: violet;
    }

    /* Layout interno (menu + cards) */
    .page-inner.page-wrapper{
      display: grid;
      grid-template-columns: clamp(180px, 22vw, 300px) minmax(0, 1fr);
      gap: 16px;
      align-items: start;
    }

    .produto-info-home {
      font-size: 0.8rem;
      line-height: 1.25;
      margin-top: 0.25rem;
      padding-top: 0.25rem;
      border-top: 1px solid #eee;
    }
    .pih-row {
      display: flex;
      gap: 2px;
      margin-bottom: 0.1rem;
    }

    .pih-label {
      color: #777;
      font-weight: 500;
      min-width: 3.4rem;
      font-size: 0.9rem;
    }

    

    .pih-row .pih-label + .pih-label{
       font-size: 1.1rem;   /* antes estava 1.4rem */
       font-weight: 450;
       color: #444;
    }

    .pih-row:last-child .pih-label{
       font-size: 1.3rem;
       font-weight: 700;
       color: #111;
    }

    .pih-row:last-child{
        margin-top: 0.25rem;
    }

    /*resultado da busca*/
    .card-title,
    .pih-value{
      color: #bd1c1c;
      font-weight: 450;
      font-size: 0.8rem;
    }

    .card-product{
      display: flex;
      flex-direction: column;
      height: 100%;
    }

    .card-product .card-body{
      display: flex;
      flex-direction: column;
      flex: 1;
    }

    .card-product .pih-desc{
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      line-height: 1.25;
      min-height: calc(1.25em * 2);
    }

    .card-product .pih-value {
        cursor: help;
    }

    .card-product .btn-detalhe {
        padding: 2px 18px;      /* ↓ altura (vertical) | ↑ largura (horizontal) */
        font-size: 0.8rem;      /* levemente menor */
        line-height: 1.2;
        min-width: 110px;       /* garante uma largura mínima */
        text-align: center;
        width: 80%;
        margin: auto;
        display: block;
        margin-left: auto;
        margin-right: auto;
        /*margin-top: auto;   /* empurra pro fundo */
    }
</style>
<style>
    /* Caixa de busca centralizada */
  

  .busca-sugestoes .sug-item {
    display: block;
    width: 100%;
    text-align: left;
    padding: 4px 8px;
    border: none;
    background: #fff;
    cursor: pointer;
    font-size: 0.85rem;
  }

  .busca-sugestoes .sug-item small {
    color: #888;
  }

  .busca-sugestoes .sug-item:hover {
    background: #f1f1f1;
  }

  .card-product a.d-block{
      display:block;
      height: 230px;           /* desktop */
      background:#fff;
    }

    /* Imagem encaixa sem cortar */
    .card-product a.d-block img{
      width: 100%;
      height: 100%;
      object-fit: contain;     /* <- NÃO CORTA */
      display: block;
    }

  .busca-sugestoes .sug-item {
    display: block;
    width: 100%;
    text-align: left;
    padding: 4px 8px;
    border: none;
    background: #fff;
    cursor: pointer;
    font-size: 0.85rem;
  }

  .busca-sugestoes .sug-item small {
    color: #888;
  }

  .busca-sugestoes .sug-item:hover {
    background: #f1f1f1;
  }

  /* Container geral da busca */
  .search-loja-wrapper {
    max-width: 700px;
    margin: 0 auto 14px;
    text-align: center;
     position: relative;
  }

  /* Título em cima da caixa */
  .search-loja-label {
    display: block;
    font-weight: 600;
    font-size: 0.95rem;
    text-transform: uppercase;
    letter-spacing: .05em;
    color: #0b6fa4;
    margin-bottom: 4px;
  }

  /* “Pílula” azul envolvendo o input + lupa */
  .search-loja-inner {
    display: flex;
    align-items: center;
    gap: 6px;
    background: #0b6fa4;
    border-radius: 2px;
    padding: 4px 10px;
    box-shadow: 0 2px 6px rgba(0,0,0,.25);
  }

  /* Lupa (unicode – não precisa de fonte extra) */
  .search-loja-icon {
    color: #ffffff;
    font-size: 1rem;
    flex: 0 0 auto;
  }

  /* Próprio campo de texto */
  .search-loja-input {
    flex: 1;
    border: none;
    outline: none;
    border-radius: 2px;
    padding: 6px 10px;
    font-size: 0.9rem;
  }

  /* remove borda “default” do input (para não brigar com o azul) */
  .search-loja-input:focus {
    box-shadow: none;
  }

  /* placeholder mais clarinho */
  .search-loja-input::placeholder {
    color: #999;
  }

  /*!-- ///////////////////////////////////////////////////////////////////////////////// --}}*/
  /* Dropdown de sugestões continua funcionando */
  .busca-sugestoes {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    max-width: 700px;
    background: #fff;
    border: 1px solid #ccc;
    border-top: none;
    z-index: 30;
    max-height: 260px;
    overflow-y: auto;
    display: none;
  }
  
  .search-input,
  #searchInput,
  input[type="search"] {
    border-radius: 0 !important;
  }

  .search-wrapper,
  .search-box {
    border-radius: 0 !important;
  }

  /* descrição */
  .sug-desc {
    font-weight: 600;
    white-space: normal;
    word-break: break-word;
  }

  /* preço */
  .sug-preco {
    text-align: right;
    font-weight: 700;
    color: #0a7c2f;
    white-space: nowrap;
  }

  /* código */
  .sug-codigo {
    text-align: right;
    font-size: 11px;
    color: #666;
  }
  /*////////////////////////////////////////////////////////////////////////////////
        COMEÇOU AQUI
  ////////////////////////////////////////////////////////////////////////////////*/
  /* caixa da lista (sugestões) - aumenta largura */
  #sugBox, .sug-box {
    width: 620px;            /* ajuste aqui */
    max-width: 92vw;
  }

  /* ITEM DA SUGESTÃO */
  .sug-item {
    display: grid;
    grid-template-columns: 1fr 180px 120px; /* descrição | preço | código */
    gap: 10px;
    padding: 8px 10px;
    font-size: 12px;
    align-items: center;
    border-bottom: 1px solid #e5e5e5;
    background: #0a7c2f;
    color:#dd1111;
  }

  /* tipografia menor p/ caber */
  .sug-desc{ font-size: 13px;color:burlywood }
  .sug-preco, .sug-codigo{
    font-size: 12px;
    text-align: right;
    white-space: nowrap;
    color:#0a7c2f;
  }

  .search-loja-inner { border-radius: 0 !important; }
  .search-loja-input { border-radius: 0 !important; }

  /* dropdown alinhado com a caixa de busca e mais largo */
  #buscaLojaSugestoes{
    width: 760px;          /* AUMENTA AQUI */
    max-width: 92vw;
    left: 50%;
    transform: translateX(-50%);
    right: auto;
    border: 1px solid #ccc;
    border-top: none;
    background: #fff;
  }

  /* item com 3 colunas */
  #buscaLojaSugestoes .sug-item{
    display: grid;
    grid-template-columns: 1fr 120px 90px; /* desc | preço | código */
    gap: 10px;
    padding: 8px 10px;
    align-items: center;
    border-bottom: 1px solid #eee;
    background: #fff;
    color: #222;
    cursor: pointer;
    font-size: 13px;
  }

  /* tipografia e alinhamentos */
  #buscaLojaSugestoes .sug-desc{ font-weight: 600; }
  #buscaLojaSugestoes .sug-preco{ text-align:right; font-weight:700; white-space:nowrap; color:#0a7c2f; }
  #buscaLojaSugestoes .sug-codigo{ text-align:right; white-space:nowrap; color:#666; font-size:12px; }

  #buscaLojaSugestoes .sug-item:hover{ background:#f4f4f4; }

  @media (max-width: 1200px){
    .cards-row{ grid-template-columns: repeat(3, minmax(220px, 1fr)); }
  }

  @media (min-width: 993px){
    .page-wrapper aside{
      position: sticky;
      top: 12px;
      align-self: start;
    }
  }

  @media (max-width: 992px){
    .page-wrapper{ grid-template-columns: 1fr; } /* menu em cima, cards embaixo */
    .cards-row{ grid-template-columns: repeat(2, minmax(220px, 1fr)); }

    .btn-menu-mobile{
      display: flex;
      align-items: center;
      justify-content: center;
    }

    #btnMenuMobile.btn-menu-mobile{
      display: inline-flex !important;
      align-items: center;
      justify-content: center;
    }

    .card-product .pih-value{
      -webkit-line-clamp: 2;
      min-height: calc(1.25em * 2);
    }
  }

  /* MENU (drawer) */
  @media (max-width: 991.98px){
    #menuLateral{
      width: min(320px, 86vw);   /* se adapta */
      max-width: 86vw;
      height: 100vh;
      overflow-y: auto;          /* rola por dentro */
      -webkit-overflow-scrolling: touch;
    }
  }

  @media (max-width: 991px){
    .page-wrapper{
      grid-template-columns: 1fr;   /* menu em cima, conteúdo embaixo */
    }
  }

  @media (min-width: 600px) and (max-width: 991.98px){
    #menuLateral{ width: min(380px, 70vw); }
  }

  /* MOBILE: aumenta legibilidade sem bagunçar desktop */
  @media (max-width: 576px){

    /* nome/descrição do produto */
    .pih-value{
      font-size: 1.1rem;
      line-height: 1.25;
    }

    /* labels "código" / "à vista" */
    .pih-label{
      font-size: 1rem;
    }

    /* valores (01 / R$ ...) */
    .pih-row .pih-label + .pih-label{
      font-size: 1.1rem;
    }

    /* botão mais confortável no dedo */
    .card-product .btn-detalhe{
      font-size: 0.95rem;
      padding: 6px 14px;
    }

    .card-product .pih-value{
      -webkit-line-clamp: 3;
      min-height: calc(1.25em * 3);
    }

    .card-product img{
      height: 170px;      /* 🟢 menor no celular */
      object-fit: cover;
    }

    .cards-row{
      gap: 10px;          /* 🟢 mais compacto */
    }

    .card-product a.d-block{
      height: 170px;         /* ajuste fino: 160~190 */
    }

    .pih.row{
       margin-bottom: 0.25rem;
       gap:8px
    }

    .card-product .pih-value{
      -webkit-line-clamp: 3;
      min-height: calc(1.25em * 3);
    }

    .card-product .card-body{
      padding-left: 18px;
      padding-right: 18px;
    }

    .card-product .card-body > .pih-row:first-child .pih-value{
      -webkit-line-clamp: 3;
      min-height: calc(1.25em * 3);
      font-size: 1.08rem;
    }

    .card-product .pih-titulo .pih-value{
    -webkit-line-clamp: 3;
    min-height: calc(1.25em * 3);
  }
  }


  @media (max-width: 520px){
  .cards-row{ grid-template-columns: repeat(2, minmax(0, 1fr)) !important; }
}


  @media (max-width: 360px){
    .page-wrapper .cards-row{
      grid-template-columns: 1fr;
    }
  }
</style>
<style>
   /* ===== BOTÃO ☰ (só aparece no mobile) ===== */
  .btn-menu-mobile{
    display:none;
    border:0;
    background:#0b6fa4;
    color:#fff;
    font-size:20px;
    padding:6px 12px;
    line-height:1;
    box-shadow: 0 2px 6px rgba(0,0,0,.25);
    margin-bottom:8px;
    border-radius: 6px;
  }

  /* backdrop */
  .menu-backdrop{
    display:none;
    position:fixed;
    inset:0;
    background: rgba(0,0,0,.35);
    z-index: 1040;
  }

  /* botão X no menu */
  .btn-fechar-menu{
    display:none;           /* só mobile */
    border:0;
    background:transparent;
    font-size:22px;
    line-height:1;
    padding:10px 12px;
    cursor:pointer;
  }

  /* ===== MOBILE/TABLET: vira drawer ===== */
  @media (max-width: 991px){

    /* botão ☰ aparece */
    .btn-menu-mobile{ display:inline-flex; align-items:center; justify-content:center; }

    /* seu layout vira 1 coluna (conteúdo) */
    .page-wrapper{
      grid-template-columns: 1fr !important;
    }

    /* ASIDE vira drawer fixo */
    .page-wrapper aside{
      position: fixed;
      top: 0;
      left: 0;
      height: 100vh;
      width: min(320px, 86vw);
      background: #fff;
      z-index: 1050;

      transform: translateX(-110%);
      transition: transform .22s ease;

      padding: 10px;
      overflow-y: auto;
      box-shadow: 8px 0 22px rgba(0,0,0,.25);
    }

    .btn-fechar-menu{ display:block; }

    /* quando abre */
    body.menu-open .page-wrapper aside{
      transform: translateX(0);
    }
    body.menu-open .menu-backdrop{
      display:block;
    }

    /* trava scroll do body ao abrir */
    body.menu-open{
      overflow:hidden;
    }
  }

  .top-bar-loja{
    display: flex;
    align-items: center;
    gap: 12px;
  }

  /* busca ocupa todo espaço restante */
  .search-loja-wrapper{
    flex: 1;
  }
</style>
<style>
  /* ===== Card base (não quebra seu layout) ===== */
  .card-product .card-body{
    padding: 12px 14px;
  }

    /* Nome/descrição (primeira informação forte) */
   
     /* só a descrição (título do card) */
 .card-product .pih-titulo .pih-value{
   font-weight: 500;
   color: #111;
   line-height: 1.25;
 }

 /* valores do bloco (código e preço) ficam normais */
 .card-product .produto-info-home .pih-value{
   font-weight: 450;
   color: #222;
 }


    /* Labels menores e mais “cinza” */
    .card-product .pih-label{
      color: #7a7a7a;
      font-weight: 600;
      font-size: 0.95rem;
      min-width: 3.2rem; /* mantém alinhado "cód.", "à vista" */
    }

   .card-product .pih-titulo .pih-value{
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      min-height: calc(1.25em * 2);
    }


    /* Valores (código e preço) */
    .card-product .produto-info-home .pih-row .pih-value{
      color: #222;
      font-weight: 450;
    }

    /* Dá um destaque no preço SEM aumentar o card */
    .card-product .produto-info-home .pih-row:nth-child(2) .pih-value{
      font-weight: 500;
      color: #0b0b0b;
    }

    /* Mantém o botão sempre no mesmo lugar do card */
    .card-product{
      display: flex;
      flex-direction: column;
    }

    .card-product .card-body{
      display: flex;
      flex-direction: column;
      flex: 1;
    }

    .card-product .produto-info-home{
      margin-top: auto; /* empurra o bloco do botão para baixo */
    }

    /* Botão com cara de marketplace */
    .card-product .btn-detalhe{
      padding: 8px 14px;
      border-radius: 8px;
      font-weight: 700;
      width: 100%;
      max-width: 240px;
    }

    /* Centraliza sem esticar demais */
    .card-product .mt-1{
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }
</style>
<style>
  /* (A) Descrição do produto (topo do card) */
.card-product .card-body > .pih-row:first-child .pih-value{
  font-size: 1.05rem;
  font-weight: 500;
  color:#222;

  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;

  line-height: 1.25;
  min-height: calc(1.25em * 2); /* reserva 2 linhas -> cards iguais */
}

/* (B) Valores (código e preço) dentro do bloco de info */
.card-product .produto-info-home .pih-value{
  font-size: 1.05rem;
  font-weight: 500;
  color:#eb0505;
}

/* 🟢 (BASE) melhora leitura em tudo */
.card-product .card-body{
  padding-left: 18px;
  padding-right: 18px;
}

.card-product .produto-info-home{
  margin-top: 10px;
}

.pih-row{
  gap: 6px;
  margin-bottom: 0.25rem;
}

</style>
<style>
  /* ===================== PREMIUM (LIGHT) ===================== */

/* dá mais “respiro” e leitura */
.card-product .card-body{
  padding: 14px 16px;
}

/* título/descrição: mais forte e previsível */
/*.card-product .pih-value{
  font-weight: 700;
  color: #111;
  line-height: 1.25;
}*/

/* labels (cód, à vista) mais discretos */
.card-product .pih-label{
  color: #7a7a7a;
  font-weight: 600;
  font-size: .92rem;
}

/* valor do preço destacado (2ª linha do bloco info) */
.card-product .produto-info-home .pih-row:nth-child(2) .pih-value{
  font-weight: 500;
}

/* garante que o bloco de info “cola” no fundo e o botão fica alinhado */
.card-product{
  display: flex;
  flex-direction: column;
}
.card-product .card-body{
  display: flex;
  flex-direction: column;
  flex: 1;
}
.card-product .produto-info-home{
  margin-top: auto;
}

/* card com “acabamento” */
.card-product{
  border: 1px solid #eee;
  border-radius: 10px;
  overflow: hidden;
  transition: transform .12s ease, box-shadow .12s ease;
}
@media (hover:hover){
  .card-product:hover{
    transform: translateY(-2px);
    box-shadow: 0 10px 22px rgba(0,0,0,.10);
  }
}

/* imagem com fundo neutro p/ ficar sempre apresentável */
.card-product a.d-block{
  background: #fafafa;
}
.card-product img{
  background: #fafafa;
}

/* botão com “peso” melhor */
.card-product .btn-detalhe{
  font-weight: 700;
  border-radius: 8px;
  width: 100%;
  max-width: 240px;
}
.card-product .mt-1{
  display:flex;
  justify-content:center;
  margin-top: 10px;
}

</style>
<style>
  /* ===== HEADER AJUSTADA ===== */
/* ===== HEADER 2 LINHAS (3 colunas) ===== */
.hdr{
  width: 100%;
  padding: 10px 14px;
  box-sizing: border-box;
}

/* cada linha tem 3 divisões */
.hdr-row{
  display: flex;
  align-items: center;
  gap: 14px;
}

/* 3 colunas: esquerda fixa, meio flexível, direita flexível (vazio) */
.hdr-col.left, .hdr-col.right { width: 120px; flex: 0 0 120px; }
.hdr-col.mid{ flex: 1 1 auto; min-width:0; display:flex; justify-content:center; }


/* LINHA 1 */
.hdr-row1{ margin-bottom: 10px; }

.hdr-logo{
  width: 64px;
  height: 44px;
  object-fit: contain;
  display: block;
}

/* título */
.hdr-title{
  color: #fff;
  font-weight: 800;
  font-size: 26px;
  line-height: 1.1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.hdr-title{
  text-align: center;
  width: 100%;
}

/* LINHA 2 */
.hdr-row2{}

/* hamburger */
.hdr-burger{
  width: 48px;
  height: 42px;
  border: 0;
  border-radius: 10px;
  background: rgba(255,255,255,.16);
  color: #fff;
  font-size: 22px;
  cursor: pointer;
  background:#0d6efd;
}
.hdr-burger:hover{ background: rgba(255,255,255,.24); }

/* busca grande */
.hdr-searchWrap{
  position: relative;
  display: flex;
  align-items: center;
  gap: 10px;
  background: #ffffff;
  border-radius: 5px;
  padding: 10px 12px;
  box-shadow: 0 6px 16px rgba(0,0,0,.18);
}

.hdr-searchIcon{ opacity: .75; font-size: 18px; }

.hdr-search{
  width: 100%;
  border: 0;
  outline: none;
  font-size: 16px;
  background: transparent;
}

/* sugestões */
.hdr-sugestoes{
  position: absolute;
  top: calc(100% + 8px);
  left: 0;
  right: 0;
  background: #fff;
  border-radius: 10px;
  box-shadow: 0 10px 22px rgba(0,0,0,.18);
  overflow: hidden;
  z-index: 2000;
  display: none;
}
.hdr-sugestoes.show{ display:block; }

/* responsivo: no mobile, tira o “vazio” da direita */
@media (max-width: 768px){
  .hdr-col.right{ display:none; }
  .hdr-col.left{ width: 60px; flex: 0 0 60px; }
  .hdr-title{ font-size: 22px; }
}

.hdr-searchWrap{
  max-width: 900px;     /* ajuste se quiser maior/menor */
  margin: 0 auto;       /* centraliza */
}

.hdr{
  padding: 6px 14px;    /* era 10px 14px */
}

.hdr-row1{
  margin-bottom: 6px;   /* era 10px */
}

.hdr-searchWrap{
  padding: 8px 10px;    /* era 10px 12px */
}
/*////////////////////////////////////////////////////////////////////////////////////*/
/* ===== Header v2 (pxh-*) SEM CONFLITO ===== */
.pxh{
  background: var(--pxh-bg, #0069a8);
  width: 100%;
  padding: 10px 14px;
  box-sizing: border-box;
}

/* container */
.pxh__wrap{
  max-width: 1280px;
  margin: 0 auto;
}

/* 2 linhas */
.pxh__row{
  display: grid;
  grid-template-columns: 84px minmax(0, 1fr) 1fr; /* left | center | right (vazio proporcional) */
  align-items: center;
  column-gap: 14px;
}

.pxh__row--top{ margin-bottom: 8px; }

/* colunas */
.pxh__left{ display:flex; align-items:center; justify-content:flex-start; }
.pxh__center{ min-width:0; }
.pxh__right{}

/* logo */
.pxh__logo{
  width: 68px;
  height: 40px;
  object-fit: contain;
  display: block;
}

/* título CENTRAL DE VERDADE */
.pxh__title{
  color: #fff;
  font-weight: 800;
  font-size: 26px;
  line-height: 1.1;
  text-align: center;

  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;

  padding: 2px 8px;
}

/* burger */
.pxh__burger{
  width: 46px;
  height: 38px;
  border: 0;
  border-radius: 10px;
  background: rgba(255,255,255,.16);
  color: #fff;
  font-size: 22px;
  cursor: pointer;
}
.pxh__burger:hover{ background: rgba(255,255,255,.24); }

/* busca (BAIXA e bonita) */
.pxh__search{
  position: relative;
  width: 100%;
  max-width: 880px;        /* controla largura bonita */
  margin: 0 auto;          /* CENTRALIZA a busca */
  background: #fff;
  border-radius: 12px;
  padding: 6px 10px;       /* <<< AQUI diminui altura real */
  display: flex;
  align-items: center;
  gap: 10px;
  box-shadow: 0 8px 18px rgba(0,0,0,.18);
}

/* ícone não aumenta altura */
.pxh__icon{
  font-size: 18px;
  line-height: 1;
  opacity: .75;
}

/* input BAIXO (sem altura “fantasma”) */
.pxh__input{
  width: 100%;
  border: 0;
  outline: none;
  background: transparent;

  height: 28px;            /* <<< TRAVA altura */
  line-height: 28px;       /* <<< TRAVA altura */
  padding: 0;              /* <<< remove padding herdado */
  margin: 0;               /* <<< remove margin herdado */
  font-size: 16px;
}

/* sugestões */
.pxh__sug{
  position: absolute;
  top: calc(100% + 8px);
  left: 0;
  right: 0;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 12px 24px rgba(0,0,0,.2);
  overflow: hidden;
  z-index: 2500;
  display: none;
}
.pxh__sug.show{ display:block; }

/* MOBILE */
@media (max-width: 768px){
  .pxh{ padding: 10px 12px; }
  .pxh__row{
    grid-template-columns: 64px minmax(0,1fr);
  }
  .pxh__right{ display:none; }
  .pxh__title{ font-size: 22px; text-align:left; }
  .pxh__search{ max-width: 100%; }
}

.hdr-col.mid{
  flex: 1 1 auto;
  min-width: 0;
  display: flex;
  justify-content: center;   /* centraliza a busca */
}

/* aumenta a largura da busca */
.hdr-searchWrap{
  width: min(980px, 100%);   /* <-- AQUI aumenta (troque 980 se quiser) */
  margin: 0 auto;
}

.hdr-searchWrap{              /* a “caixa vermelha” */
  padding: 4px 10px;          /* diminui altura do container */
  border-radius: 12px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.hdr-search{                  /* o input */
  height: 26px;               /* <<< trava altura */
  line-height: 26px;          /* <<< trava */
  padding: 0 !important;      /* <<< mata padding herdado */
  margin: 0 !important;
  border: 0;
  outline: none;
  font-size: 16px;
  background: transparent;
}

.hdr-searchIcon{              /* o ícone */
  line-height: 1;             /* <<< impede “empurrar” altura */
}

#headerLojaExclusiva input{
  height: 26px !important;
  line-height: 26px !important;
  padding: 0 !important;
}

.produtos-grid{
  display: grid;
  gap: 14px;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
}

/* MOBILE: força 2 colunas */
@media (max-width: 576px){
  .produtos-grid{
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
  }
}

.produtos-grid .card img,
.produtos-grid .produto-img{
  width: 100%;
  height: 190px;      /* aumenta */
  object-fit: contain;
}
@media (max-width: 576px){
  .produtos-grid .card img,
  .produtos-grid .produto-img{
    height: 160px;
  }
}

/* desktop: menu lateral normal */
.menu-lateral{
  display: block;
}

/* mobile: vira drawer */
@media (max-width: 992px){
  .menu-lateral{
    position: fixed;
    top: 0;
    left: 0;
    width: 82%;
    max-width: 320px;
    height: 100vh;
    background: #fff;
    z-index: 3000;
    transform: translateX(-105%);
    transition: transform .2s ease;
    box-shadow: 10px 0 24px rgba(0,0,0,.25);
    overflow: auto;
    padding: 12px;
  }
  .menu-lateral.show{
    transform: translateX(0);
  }

  .menu-backdrop{
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,.35);
    z-index: 2990;
    display: none;
  }

  
}

@media (max-width: 992px){
  .menu-lateral{
    transform: translateX(-105%);
  }
}

/*@media (max-width: 992px){
  #menuXDepartamentos{
    transform: translateX(-105%) !important;
    position: fixed !important;
    left: 0;
    top: 0;
    height: 100vh;
    z-index: 3000;
  }

  
}*/

.menu-drawer-head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
  padding: 10px 6px;
  border-bottom: 1px solid rgba(0,0,0,.12);
  margin-bottom: 10px;
}

.menu-drawer-title{
  font-weight: 800;
  font-size: 16px;
  line-height: 1.1;
  overflow:hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: #0d6efd;
}

.btn-fechar-menu{
  border: 0;
  background: transparent;
  font-size: 20px;
  line-height: 1;
  padding: 6px 10px;
  cursor: pointer;
}

/* DESKTOP: não existe drawer/backdrop */
@media (min-width: 992px){
  /*.deptXos-backdrop,*/
 /* .deptosX-drawer{
    display: none !important;
  }*/
  body.menu-open{
    overflow: auto !important;
  }
}
/* ===== HEADER fica sempre acima ===== */
#headerLojaExclusiva, .hdr{
  position: sticky;
  top: 0;
  z-index: 1060; /* acima do drawer/backdrop */
}

/* altura do header (ajuste se precisar) */
:root{
  --hdr-h: 104px;     /* <<< ajuste fino (ex: 96px / 110px) */
  --drawer-w: 280px;  /* <<< largura do menu (menor pra caber + colunas) */
}

/* ===== GRID de produtos igual HOME: auto-colunas por tamanho ===== */
.produtos-grid{
  display: grid;
  gap: 16px;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  align-items: stretch;
}

/* telas maiores: mais colunas possíveis */
@media (min-width: 1200px){
  .produtos-grid{ grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); }
}
@media (min-width: 1600px){
  .produtos-grid{ grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }
}

/* ===== Imagem “mais destacada” no card ===== */
.produtos-grid .card .img-wrap,
.produtos-grid .card .produto-img-wrap{
  height: 240px;              /* destaque */
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fff;
}

.produtos-grid .card img{
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
}

/* container principal (menu + produtos) */
.page-wrapper{
  display: grid;
  grid-template-columns: 240px 1fr; /* menu mais estreito = ganha coluna */
  gap: 16px;
  align-items: start;
}

/* menu lateral mais enxuto */
/*#menuXDepartamentos{
  width: 240px;
}*/

/* área dos produtos não pode ter width travando */
.produtos-area{
  min-width: 0;
}

/* imagem do card (mais destaque) */
.produto-img{
  height: 190px;
  object-fit: contain;
  display: block;
}

/* mobile: menu vira drawer (não ocupa coluna) */
@media (max-width: 991px){
  .page-wrapper{
    grid-template-columns: 1fr;
  }

  /*#menuXDepartamentos{
    width: auto;
  }*/

  .produto-img{
    height: 170px;
  }
}

/* grid do miolo: menu + produtos */
.page-wrapper{
  display: grid;
  grid-template-columns: 240px 1fr; /* diminui menu pra caber mais coluna */
  gap: 16px;
  align-items: start;
}

/* garante que o conteúdo de produtos não "trava" e não esmaga colunas */
.produtos-area{
  min-width: 0;
}

/* menu lateral mais estreito (desktop) */
/*#menuXDepartamentos{
  width: 240px;
}*/

/* mobile/tablet: menu não ocupa coluna (vira drawer) */
@media (max-width: 991px){
  .page-wrapper{
    grid-template-columns: 1fr;
  }

  /*#menuXDepartamentos{
    width: auto;
  }*/
}

.produto-img{
  height: 190px;
  object-fit: contain;
  display: block;
}

</style>
<style>
  /* ------------- MENOS MARGEM NAS BORDAS (se tiver container com padding) ------------- */
.page-shell,
.container-fluid.page-shell{
  padding-left: 8px !important;
  padding-right: 8px !important;
}

/* ------------- MENU MAIS ESTREITO NO DESKTOP ------------- */
@media (min-width: 992px){
  /* largura fixa do menu (ajuste aqui) */
  
  /*#menuXDepartamentos.menu-lateral{
    flex: 0 0 260px !important;
    max-width: 260px !important;
    width: 260px !important;
  }*/

  /* main ocupa o resto */
  main.page-main{
    flex: 1 1 auto !important;
    max-width: calc(100% - 260px) !important;
  }
}

/* opcional: menu com menos “respiro” interno */

/*#menuXDepartamentos .card{
  margin-bottom: 10px;
}*/

/* ------------- GRID AUTOMÁTICO PARA OS CARDS ------------- */
#cardsRow.cards-row{
  display: grid !important;
  grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
  gap: 16px;
  margin: 0 !important; /* mata margem negativa do .row */
}

/* mata o comportamento de colunas do Bootstrap dentro do grid */
#cardsRow.cards-row > [class*="col-"]{
  padding: 0 !important;
  margin: 0 !important;
  width: auto !important;
  max-width: none !important;
  flex: none !important;
}

.container-fluid,
.page-shell {
  padding-left: 4px !important;
  padding-right: 4px !important;
}

@media (min-width: 992px){
  
 /* #menuXDepartamentos.menu-lateral{
    width: 240px !important;
    max-width: 240px !important;
    flex: 0 0 240px !important;
  }*/

  main.page-main{
    max-width: calc(100% - 240px) !important;
  }
}

#cardsRow.cards-row{
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 14px;
}

.card-product{
  border-radius: 10px;
}

.card-product .card-body{
  padding: 10px 12px;
}

/* BODY vermelho (só pra ficar claro) */
body{
  background: #ffffff; /* ou o vermelho que você está usando */
}

/* PAGE-SHELL (verde) centralizado dentro do vermelho */
.container-fluid.page-shell{
  background: #fff;          /* seu verde */
  width: min(1400px, 96vw);     /* limita e deixa responsivo */
  margin-left: auto !important;
  margin-right: auto !important;

  /* opcional: manter padding interno “normal” */
  padding-left: 12px !important;
  padding-right: 12px !important;
}

.container-fluid.page-shell{
  width: min(1600px, 92vw);
  margin: 16px auto !important;  /* centraliza e dá respiro em cima/baixo */
  padding-left: 8px !important;
  padding-right: 8px !important;
}

.container-fluid.page-shell{
  background: #ffffff;
  width: 100%;                 /* NÃO encolhe por padrão */
  max-width: 1800px;           /* grande o suficiente p/ 5+ colunas */
  margin: 16px auto !important;/* centralizado */
  padding-left: 12px !important;
  padding-right: 12px !important;
}

/* só em telas menores eu deixo "respirar" (para aparecer o vermelho) */
@media (max-width: 992px){
  .container-fluid.page-shell{
    width: 96vw;               /* aqui pode encolher */
    max-width: none;
  }
}

@media (min-width: 1400px){
  #cardsRow.cards-row{
    grid-template-columns: repeat(5, minmax(0, 1fr)) !important;
  }
}

@media (max-width: 576px){
  #cardsRow.cards-row{
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
  }
}

/* ===== DESKTOP (normal, menu como coluna) ===== */
@media (min-width: 992px){
  
 /* #menuXDepartamentos{
    position: static !important;
    transform: none !important;
    height: auto !important;
    width: auto !important;
    max-width: none !important;
  }*/

  #menuOverlay{ display:none !important; }
}

/* ===== MOBILE (menu vira drawer e não ocupa espaço na página) ===== */
@media (max-width: 991.98px){
  /* o menu sai do fluxo (não “duplica”) */
  /*#menuXDepartamentos{
    position: fixed !important;
    top: 0;
    left: 0;
    height: 100vh !important;
    width: min(88vw, 340px) !important;
    max-width: 340px !important;
    z-index: 1050 !important;
    background: #fff !important;
    transform: translateX(-105%) !important;
    transition: transform .22s ease !important;
  }*/

  

  /* overlay */
  #menuOverlay{
    position: fixed !important;
    inset: 0 !important;
    background: rgba(0,0,0,.35) !important;
    z-index: 1040 !important;
    opacity: 0 !important;
    pointer-events: none !important;
    transition: opacity .2s ease !important;
    display: block !important;
  }
  
  

  /* main vira 100% (o menu não empurra nada) */
  main.page-main{
    width: 100% !important;
    max-width: 100% !important;
  }
}

@media (max-width: 991.98px){

  /*#menuXDepartamentos{ transform: translateX(-105%); }
  body.menu-open #menuXDepartamentos{ transform: translateX(0); }*/

  #menuBackdrop{ opacity:0; pointer-events:none; }
  body.menu-open #menuBackdrop{ opacity:1; pointer-events:auto; }
}

/* Header do drawer: título à esquerda e X pequeno à direita */
.menu-drawer-head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 10px;
  padding: 10px 12px;
}

.menu-drawer-title{
  flex: 1 1 auto;
  min-width: 0;
  font-weight: 700;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}



/* overlay/backdrop */
#menuBackdrop{
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.35);
  opacity: 0;
  pointer-events: none;
  transition: opacity .2s ease;
  z-index: 1040;
}

body.menu-open #menuBackdrop{
  opacity: 1;
  pointer-events: auto;
}

.menu-drawer-head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 10px;
}

.menu-drawer-title{
  flex: 1 1 auto;
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.btn-fechar-menu{
  width: auto !important;
  padding: 4px 8px !important;
  font-size: 18px !important;
  line-height: 1 !important;
  border: 0 !important;
  background: transparent !important;
  cursor: pointer;
  flex: 0 0 auto;
}

/* o drawer precisa estar acima do backdrop */
/*#menuXDepartamentos{
  position: fixed;
  z-index: 1050 !important;
}*/

/* backdrop abaixo do drawer */
#menuBackdrop{
  z-index: 1040 !important;
}

/* header e botão X acima de tudo dentro do drawer */
.menu-drawer-head{
  position: sticky;   /* pode ser sticky ou relative */
  top: 0;
  z-index: 1060;
  background: #fff;
}

/* garante clique */
/*#btnFecharMenu{
  pointer-events: auto !important;
  position: relative;
  z-index: 1061;
  padding: 4px 8px !important;
  font-size: 18px !important;
  line-height: 1 !important;
  border: 0 !important;
  background: transparent !important;
}*/

#menuBackdrop{
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.35);
  opacity: 0;
  pointer-events: none;
  transition: opacity .2s ease;
  z-index: 1040;
}
body.menu-open #menuBackdrop{
  opacity: 1;
  pointer-events: auto;
}

@media (max-width: 991.98px){
  /*#menuXDepartamentos{
    position: fixed;
    top: 0; left: 0;
    height: 100vh;
    width: min(88vw, 340px);
    background:#fff;
    z-index: 1050;
    transform: translateX(-105%);
    transition: transform .22s ease;
    overflow-y: auto;
  }*/

  /*body.menu-open #menuXDepartamentos{
    transform: translateX(0);
  }*/
}

.menu-drawer-head{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 10px;
}
.menu-drawer-title{
  flex: 1 1 auto;
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

#btnFecharMenu{
  width:auto;
  padding:4px 8px;
  font-size:18px;
  line-height:1;
  border:0;
  background:transparent;
  cursor:pointer;
}

</style>
<style>
  /* ======================= FIX FINAL: MENU DRAWER + BACKDROP ======================= */

/* DESKTOP: #menu_Departamentos volta ao normal (nada de fixed/transform) */
@media (min-width: 992px){
  #menuLateral{
    position: static !important;
    transform: none !important;
    top: auto !important;
    left: auto !important;
    height: auto !important;
    width: auto !important;
    max-width: none !important;
    z-index: auto !important;
    box-shadow: none !important;
    overflow: visible !important;
  }

  #menuBackdrop{
    display: none !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }

  body.menu-open{ overflow: auto !important; }
}

/* BACKDROP (sempre existe, mas só aparece quando menu-open) */
#menuBackdrop{
  position: fixed !important;
  inset: 0 !important;
  background: rgba(0,0,0,.35) !important;
  opacity: 0 !important;
  pointer-events: none !important;
  transition: opacity .2s ease !important;
  z-index: 1040 !important;
  display: block !important;
}
body.menu-open #menuBackdrop{
  opacity: 1 !important;
  pointer-events: auto !important;
}

/* MOBILE: #menuLateral vira drawer */
@media (max-width: 991.98px){
  #menuLateral{
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    height: 100vh !important;
    width: min(88vw, 340px) !important;
    max-width: 340px !important;
    background: #fff !important;

    z-index: 1050 !important;
    transform: translateX(-105%) !important;
    transition: transform .22s ease !important;

    overflow-y: auto !important;
    -webkit-overflow-scrolling: touch !important;
    box-shadow: 8px 0 22px rgba(0,0,0,.25) !important;
  }

  body.menu-open #menuLateral{
    transform: translateX(0) !important;
  }

  body.menu-open{ overflow: hidden !important; }
}

/* X menor e não “come” título */
.menu-drawer-title{ min-width: 0 !important; }
#btnFecharMenu{
  width: auto !important;
  padding: 4px 8px !important;
  font-size: 18px !important;
  line-height: 1 !important;
  border: 0 !important;
  background: transparent !important;
  cursor: pointer !important;
  flex: 0 0 auto !important;
}

/* ================= GRID DOS CARDS (ÚNICO) ================= */
#cardsRow{
  display: grid !important;
  gap: 16px;
  margin: 0 !important;      /* mata margem negativa de row */
  padding: 0 !important;
}

/* Desktop grande: 5 colunas */
@media (min-width: 1400px){
  #cardsRow{ grid-template-columns: repeat(5, minmax(0, 1fr)) !important; }
}

/* Desktop normal */
@media (min-width: 992px) and (max-width: 1399.98px){
  #cardsRow{ grid-template-columns: repeat(4, minmax(0, 1fr)) !important; }
}

/* Tablet */
@media (min-width: 576px) and (max-width: 991.98px){
  #cardsRow{ grid-template-columns: repeat(3, minmax(0, 1fr)) !important; }
}

/* Mobile: 2 colunas SEMPRE */
@media (max-width: 575.98px){
  #cardsRow{ grid-template-columns: repeat(2, minmax(0, 1fr)) !important; gap: 10px; }
}

/* o wrapper do item não pode ter “col-” */
#cardsRow .card-item{
  margin: 0 !important;
  padding: 0 !important;
}

/* ===== DESKTOP: menu e main lado a lado (NUNCA empurra pra baixo) ===== */
@media (min-width: 992px){
  /* o .row do bootstrap não pode quebrar linha por causa de width fixo */
  .page-shell > .row{
    flex-wrap: nowrap !important;
    align-items: flex-start !important;
  }

  /* menu volta ao normal (NADA de fixed/transform no desktop) */
  #menuLateral{
    position: static !important;
    transform: none !important;
    height: auto !important;

    /* largura controlada (não estoura 100%) */
    flex: 0 0 300px !important;
    max-width: 300px !important;
    width: 300px !important;
  }

  /* main ocupa o resto */
  main.page-main{
    flex: 1 1 auto !important;
    max-width: none !important;
    width: auto !important;
    min-width: 0 !important;
  }

  /* backdrop nunca aparece no desktop */
  #menuBackdrop{ display:none !important; }
  body.menu-open{ overflow:auto !important; }
}

/* ===== MOBILE: drawer de verdade ===== */
@media (max-width: 991.98px){
  #menuLateral{
    position: fixed !important;
    top: 0; left: 0;
    height: 100vh !important;
    width: min(88vw, 340px) !important;
    max-width: 340px !important;
    background:#fff !important;
    z-index: 1050 !important;

    transform: translateX(-105%) !important;
    transition: transform .22s ease !important;
    overflow-y: auto !important;
  }

  body.menu-open #menuLateral{
    transform: translateX(0) !important;
  }

  #menuBackdrop{
    display:block !important;
    opacity: 0;
    pointer-events: none;
  }

  body.menu-open #menuBackdrop{
    opacity: 1;
    pointer-events: auto;
  }
}

/* ===== FIX: drawer do menu acima do header ===== */

/* o header pode ficar sticky, mas abaixo do drawer */
#headerLojaExclusiva{
  position: sticky;
  top: 0;
  z-index: 1500 !important;
}

/* o menu (drawer) tem que ficar ACIMA do header quando aberto */
#menuLateral{
  position: fixed !important;     /* no mobile ele é drawer */
  z-index: 2000 !important;
}

/* o backdrop abaixo do menu, mas acima do resto */
#menuBackdrop{
  z-index: 1990 !important;
}

/* garante que o “cabeçalho” do drawer (título + X) esteja clicável */
#menuLateral .menu-drawer-head{
  position: sticky;
  top: 0;
  z-index: 500 !important;
  background: #fff;
}

</style>
<style>
  /* ===== DESKTOP: menu é coluna normal ===== */
@media (min-width: 992px){
  #menuLateral{
    position: static !important;
    transform: none !important;
    width: auto !important;
    height: auto !important;
    z-index: auto !important;
  }

  #menuBackdrop{
    display: none !important;
  }
}

#headerLojaExclusiva{
  position: sticky;
  top: 0;
  z-index: 1500;
}

#headerLojaExclusiva{
  position: sticky;
  top: 0;
  z-index: 1000;
}

#menuLateral .menu-drawer-title{
  position: relative;
  z-index: 1;
}

#menuLateral{
  position: relative;
  z-index: 10;
}

</style>
<style>
  /* mais espaço da logo em relação à esquerda */
#headerLojaExclusiva .hdr-col.left {
  padding-left: 18px;   /* ajuste: 12/16/18/24 conforme quiser */
}

/* se preferir direto na imagem, pode ser assim também (use um ou outro) */
/*
#headerLojaExclusiva .hdr-logo{
  margin-left: 18px;
}
*/

/* botão no canto direito da linha 1 */
#headerLojaExclusiva .hdr-row1 .hdr-col.right{
  display: flex;
  justify-content: flex-end;
  align-items: center;
  padding-right: 14px;  /* respiro da direita */
}

#headerLojaExclusiva .hdr-row{
  display:flex;
  align-items:center;
}

#headerLojaExclusiva .hdr-col.left,
#headerLojaExclusiva .hdr-col.right{
  width: 64px;          /* mesma largura nos lados */
  flex: 0 0 64px;
}

#headerLojaExclusiva .hdr-col.mid{
  flex: 1;
  text-align: center;
}
</style>
<style>
  /* menu começa escondido */
.menu-lateral {
  display: none;
}

/* quando ativo */
.menu-lateral.is-open {
  display: block;
}

/* versão overlay (recomendado) */
@media (min-width: 768px) {
  .menu-lateral.is-open {
    position: fixed;
    top: 0;
    left: 0;
    width: 280px;
    height: 100vh;
    background: #fff;
    z-index: 1200;
    box-shadow: 2px 0 12px rgba(0,0,0,.2);
  }
}
/*/////////////////////////////////////////////////*/
.cards-row{
  display: grid;
  gap: 12px;
  grid-template-columns: repeat(2, 1fr); /* mobile */
}

@media (min-width: 768px){
  .cards-row{ grid-template-columns: repeat(3, 1fr); }
}

@media (min-width: 992px){
  .cards-row{ grid-template-columns: repeat(4, 1fr); } /* desktop */
}

@media (min-width: 1400px){
  .cards-row{ grid-template-columns: repeat(5, 1fr); } /* desktop grande */
}

/* TÍTULO/DESCRIÇÃO do produto (LOJA EXCLUSIVA) */
.pih-title{
    display: block;
    font-weight: 400;      /* tira o "pesado" */
    font-size: 0.98rem;    /* menor */
    line-height: 1.2;
    color: #111;
    margin: 0 0 6px 0;

    /* evita card estourar */
    display: -webkit-box;
    -webkit-line-clamp: 2;      /* 2 linhas no máximo */
    -webkit-box-orient: vertical;
    overflow: hidden;
}

/* bloco de código/preço */
.pih-meta{ margin-top: 2px; }

.pih-line{
  display: flex;
  gap: 8px;
  align-items: baseline;
  line-height: 1.25;
  margin-top: 2px;
}

.pih-label{
  font-size: 0.85rem;
  color: #6c757d;
  font-weight: 400;   /* sem bold */
}

.pih-text{
  font-size: 0.9rem;
  color: #111;
  font-weight: 400;   /* sem bold */
}

/* PREÇO: destaque sem exagero */
.pih-price{
  font-size: 1.02rem;
  font-weight: 600;   /* levemente destacado */
  color: #111;
}

</style>
</head>
<body>

  <header id="headerLojaExclusiva" class="hdr"
  style="background: {{#if lojista.corHeader}}{{lojista.corHeader}}{{else}}#0069a8{{/if}};">

  {{!-- <!-- LINHA 1: Logo + Título + Vazio -->
  <div id="rowLinha1" class="hdr-row hdr-row1" style="">
      <div class="hdr-col left" style="">
        {{#if lojista.logoUrl}}
          <img src="{{lojista.logoUrl}}" class="hdr-logo" alt="Logo">
        {{/if}}
      </div>

      <div class="hdr-col mid">
        <div class="hdr-title" style="">
          {{#if lojista.tituloPage}}
            {{lojista.tituloPage}}
          {{else}}
            {{produto.marcaloja}}
          {{/if}}
        </div>
      </div>

      <div class="hdr-col right" style=""><!-- vazio proporcional --></div>
  </div>

  <!-- LINHA 2: Hamburger + Busca + Vazio -->
  <div id="rowLinha2" class="hdr-row hdr-row2">
    <div class="hdr-col left">
      <button type="button" id="btnMenuMobile" class="hdr-burger" aria-label="Abrir menu">☰</button>
    </div>

    <div class="hdr-col mid">
      <div class="hdr-searchWrap">
        <span class="hdr-searchIcon">🔎</span>
        <input type="text" id="buscaLoja" class="hdr-search"
               placeholder="O que você procura?" autocomplete="off">
        <div id="buscaLojaSugestoes" class="hdr-sugestoes"></div>
      </div>
    </div>

    <div class="hdr-col right" style=""><!-- vazio proporcional --></div>
  </div> --}}
               <div id="rowLinha1" class="hdr-row hdr-row1">
                      <div class="hdr-col left">
                          {{#if lojista.logoUrl}}
                            <img src="{{lojista.logoUrl}}" class="hdr-logo" alt="Logo">
                          {{/if}}
                      </div>

                      <div class="hdr-col mid">
                        <div class="hdr-title">Papelaria Soletrar</div>
                      </div>

                      <div class="hdr-col right">
                        <button type="button" id="btnMenuMobile" class="hdr-burger" aria-label="Abrir menu">≡</button>
                      </div>
               </div>
                <div id="rowLinha2" class="hdr-row hdr-row2">
                    <div class="hdr-col left"></div>

                    <div class="hdr-col mid">
                      {{!-- <div class="hdr-searchWrap"></div> --}}
                       <div class="hdr-searchWrap">
                          <span class="hdr-searchIcon">🔎</span>
                          <input type="text" id="buscaLoja" class="hdr-search"
                                placeholder="O que você procura?" autocomplete="off">
                          <div id="buscaLojaSugestoes" class="hdr-sugestoes"></div>
                      </div>
                    </div>

                    <div class="hdr-col right"></div>
                </div>

</header>
{{!-- ===================== CONTAINER PRINCIPAL ===================== --}}
<div class="container-fluid my-3 page-shell" style="border-left: 2px #9a9797 solid;">
     <div class="row g-2 gx-lg-2 align-items-start" style="background:#ffffff;padding:6px;">
      {{!-- ===================== MENU LATERAL (1 COLUNA) ===================== --}}
      <aside id="menuLateral" class="col-12 col-lg-3 menu-lateral" >
            <div class="menu-drawer-head">
              <div class="menu-drawer-title">
                {{#if lojista.tituloPage}}{{lojista.tituloPage}}{{else}}{{produto.marcaloja}}{{/if}}
              </div>

              <button type="button" id="btnFecharMenu" class="btn-fechar-menu" aria-label="Fechar menu">✕</button>
            </div>
            {{!-- DEPARTAMENTOS --}}
            <div class="card mb-3">
                <div class="card-header py-2">
                    <strong>Outros departamentos</strong>
                </div>
                <div class="list-group list-group-flush">
                    {{#each departamentosMenu}}
                    <a href="?dep={{this._id}}"
                        class="list-group-item list-group-item-action">
                        {{this.nome}}
                    </a>
                    {{/each}}
                </div>
            </div>
            {{!-- SETORES (aparecem só após escolher DEP) --}}
            {{#if setoresMenu.length}}
            <div class="card mb-3">
                <div class="card-header py-2">
                <strong>Setores</strong>
                </div>
                <div class="list-group list-group-flush">
                {{#each setoresMenu}}
                    <a href="?dep={{../depSelecionado}}&setor={{this._id}}"
                    class="list-group-item list-group-item-action">
                    {{this.nome}}
                    </a>
                {{/each}}
                </div>
            </div>
            {{/if}}
            {{!-- SEÇÕES (aparecem só após escolher SETOR) --}}
            {{#if secoesMenu.length}}
            <div class="card mb-3">
                <div class="card-header py-2">
                <strong>Seções</strong>
                </div>
                <div class="list-group list-group-flush">
                {{#each secoesMenu}}
                    <a href="?dep={{../depSelecionado}}&setor={{../setorSelecionado}}&secao={{this._id}}"
                    class="list-group-item list-group-item-action">
                    {{this.nome}}
                    </a>
                {{/each}}
                </div>
            </div>
            {{/if}}
      </aside>
      {{!-- ===================== CARDS (5 COLUNAS) ===================== --}}
      <main id="rowcards" class="col-12 col-lg-9 page-main">
            <div class="cards-row" id="cardsRow">
              {{#if produtosLoja.length}}
                {{#each produtosLoja}}
                  <div class="card-item">
                    <article class="card h-100 card-product" style="background:#ffffff;">
                          <a href="/home-detalhe/{{this._id}}" class="d-block">
                            <img
                              class="w-100 p-2 produto-img"
                              src="{{this.pageurls.[0]}}"
                              alt="{{this.descricao}}"
                              loading="lazy"
                            >
                          </a>
                      <div class="card-body" style="background:#ffffff;">
                          <div class="pih-row">
                            <span class="pih-title">
                              {{this.descricao}} {{this.complete}}
                            </span>
                          </div>
                          <div class="pih-meta">
                                <div class="pih-line">
                                            <span class="pih-label">código :</span>
                                            <span class="pih-text">{{this.codigo}}</span>
                                </div>
                                <div class="pih-line">
                                  <span class="pih-label">à vista</span>
                                  <span class="pih-price">
                                    {{#if this.precoprazo}}
                                      R$ {{formatCurrency this.precoprazo}}
                                    {{else}}
                                      sob consulta
                                    {{/if}}
                                  </span>
                                </div>
                                <div class="mt-1">
                                  <a class="btn btn-sm btn-outline-primary btn-detalhe"
                                    href="/home-detalhe/{{this._id}}">
                                    Detalhes / vendedor
                                  </a>
                                </div>
                          </div>
                      </div>
                    </article>
                  </div>
                {{/each}}
              {{else}}
                <div class="col-12">
                  <p class="text-muted">Nenhum produto encontrado para este filtro.</p>
                </div>
              {{/if}}

            </div>
      </main>
  </div>
  <div id="menuBackdrop" class="menu-backdrop" aria-hidden="true"></div>
<script>
/*  VAI BUSCAR OS PRODUTOS DA LISTA DE SUGESTÃO*/
(function () {
  const lojaId   = '{{produto.loja_id}}';
  const input    = document.getElementById('buscaLoja');
  const sugBox   = document.getElementById('buscaLojaSugestoes');
  const cardsRow = document.getElementById('cardsRow');
  if (!input || !sugBox || !cardsRow) return;

  const originalHTML = cardsRow.innerHTML;  // salva os cards originais
  const placeholderImg = '';                // se quiser, coloque uma URL de placeholder
  let timer = null;

  // ================== FUNÇÃO AUXILIAR PARA MONTAR CARDS ==================
  async function carregarCards(q) {
    if (!q || q.trim().length < 3) {
      // volta pros cards originais
      cardsRow.innerHTML = originalHTML;
      return;
    }

    try {
      const url = `/home-page-exclusiva/busca?loja=${encodeURIComponent(lojaId)}&q=${encodeURIComponent(q.trim())}`;
      const resp = await fetch(url);
      const itens = await resp.json();
      console.log('itens',itens)
      if (!Array.isArray(itens) || itens.length === 0) {
        cardsRow.innerHTML = `
          <div class="col-12">
            <p class="text-muted mb-0">Nenhum produto encontrado para "${q}".</p>
          </div>`;
        return;
      }

      const html = itens.map(p => {
        const img = (p.pageurls && p.pageurls[0]) ? p.pageurls[0] : placeholderImg;
        const preco = p.precoprazo
          ? 'R$ ' + Number(p.precoprazo).toLocaleString('pt-BR', { minimumFractionDigits: 2 })
          : '0,00';

        return `
          <div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-3">
            <article class="card h-100 card-product">
              <a href="/produto/${p._id}" class="d-block">
                <img class="w-100 p-2"
                     src="${img}"
                     alt="${p.descricao || ''}"
                     loading="lazy">
              </a>

              <div class="card-body">
                <h6 class="card-title mb-2 text-truncate"
                    title="${p.descricao || ''}">
                  ${p.descricao || ''}
                </h6>

                <div class="produto-info-home">
                  <div class="pih-row">
                    <span class="pih-label">código.</span>
                    <span class="pih-label">${p.codigo || ''}</span>
                  </div>

                  <div class="pih-row">
                    <span class="pih-label">à vista</span>
                    <span class="pih-label">${preco}</span>
                  </div>

                  <div class="pih-row">
                    <span class="pih-label">descrição</span>
                    <span class="pih-value">
                      ${p.descricao || ''} ${p.complete || ''}
                    </span>
                  </div>
                  <div class="mt-1">
                    <a class="btn btn-sm btn-outline-primary btn-detalhe"
                       href="/home-detalhe/${p._id}">
                      Detalhes
                    </a>
                  </div>
                </div>
              </div>
            </article>
          </div>`;
      }).join('');

      cardsRow.innerHTML = html;
    } catch (e) {
      console.error('Erro na busca AJAX:', e);
    }
  }

  // ================== INPUT: APENAS MOSTRA SUGESTÕES ==================
  input.addEventListener('input', function () {
    const q = this.value.trim();
    clearTimeout(timer);

    if (q.length < 3) {
      sugBox.innerHTML = '';
      sugBox.style.display = 'none';
      // NÃO mexe nos cards aqui!
      return;
    }

    timer = setTimeout(async () => {
      try {
        const url = `/home-page-exclusiva/busca?loja=${encodeURIComponent(lojaId)}&q=${encodeURIComponent(q)}`;
        const resp = await fetch(url);
        const itens = await resp.json();
        console.log('');
        console.log('3000',itens);
        console.log('');
        if (!Array.isArray(itens) || itens.length === 0) {
          sugBox.innerHTML = '';
          sugBox.style.display = 'none';
          return;
        }

        // escape básico pra não quebrar o HTML (e evitar XSS)
        function esc(v = '') {
          return String(v).replace(/[&<>"']/g, (ch) => ({
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#39;',
          }[ch]));
        }


        // monta lista de sugestões (sem mexer nos cards)
        sugBox.innerHTML = itens.map(p => `
            <div class="sug-item"
              data-desc="${esc(p.descricao || '')}"
              data-codigo="${esc(p.codigo || '')}"
              data-preco="${esc(p.precoprazo || '')}">
              <div class="sug-desc">${esc(p.descricao || '')}</div>
              <div class="sug-preco">${p.precoprazo ? esc(p.precoprazo) : '0,00'}</div>
              <div class="sug-codigo">(${esc(p.codigo || '')})</div>
          </div>
        `).join('');

        sugBox.style.display = 'block';
      } catch (e) {
        console.error(e);
      }
    }, 250);
  });

  // ================== CLIQUE NA SUGESTÃO: AÍ SIM BUSCA E TROCA OS CARDS ==================
  sugBox.addEventListener('click', function (ev) {
    const btn = ev.target.closest('.sug-item');
    if (!btn) return;

    const desc = btn.dataset.desc || '';
    input.value = desc;        // coloca o texto escolhido no input
    sugBox.style.display = 'none';

    // agora sim: limpa e recarrega os cards só com o resultado dessa descrição
    carregarCards(desc);
  });

  // clicar fora fecha o dropdown
  document.addEventListener('click', function (ev) {
    if (ev.target !== input && !sugBox.contains(ev.target)) {
      sugBox.style.display = 'none';
    }
  });
})();
</script>
{{!-- /* ABRIR E FECHAR MENU HAMBURGER*/ --}}
<script>
(function(){
  const btnOpen  = document.getElementById('btnMenuMobile');
  const btnClose = document.getElementById('btnFecharMenu');
  const backdrop = document.getElementById('menuBackdrop'); // opcional
  const aside    = document.getElementById('menuLateral');

  if(!btnOpen || !btnClose || !aside) return;

  function abrir(){
    document.body.classList.add('menu-open');
  }
  function fechar(){
    document.body.classList.remove('menu-open');
  }

  btnOpen.addEventListener('click', abrir);
  btnClose.addEventListener('click', fechar);
  if(backdrop) backdrop.addEventListener('click', fechar);

  document.addEventListener('keydown', (e)=>{
    if(e.key === 'Escape') fechar();
  });

  aside.addEventListener('click', (e)=>{
    const a = e.target.closest('a');
    if(a && window.matchMedia('(max-width: 991px)').matches){
      fechar();
    }
  });

  window.addEventListener('resize', ()=>{
    if(window.innerWidth >= 992) fechar();
  });
})();


</script>
<script>
const sug = document.getElementById("buscaLojaSugestoes");
   sug.classList.toggle("show", sug.innerHTML.trim().length > 0);
</script>
<script>
document.addEventListener('DOMContentLoaded', () => {
  const btn = document.getElementById('btnMenuMobile');
  const menu = document.getElementById('menuLateral');

  if (!btn || !menu) return;

  btn.addEventListener('click', () => {
    menu.classList.toggle('is-open');
  });

  // opcional: fechar ao clicar fora
  document.addEventListener('click', (e) => {
    if (!menu.contains(e.target) && !btn.contains(e.target)) {
      menu.classList.remove('is-open');
    }
  });
});
</script>

</body>
</html>
