<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Admin HOME Layout</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">

  <h3 class="mb-3">Admin — HOME Layout</h3>

  <p class="text-muted mb-4">
    Aqui você altera: Hero / Destaques / Publicidade lateral.
    As imagens são enviadas do computador para o Spaces e a URL é gravada no Mongo (HomeLayout.slots).
  </p>

  <div class="row g-3">
    {{#each home.slots}}
      <div class="col-12">
        <div class="card">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
              <div>
                <span class="badge bg-dark">{{this.tipo}}</span>
                <span class="ms-2 text-muted">ordem: {{this.ordem}}</span>
              </div>
              <button class="btn btn-sm btn-outline-danger btnRemoverSlot" data-id="{{this._id}}">remover</button>
            </div>

            <div class="row g-3 align-items-center">
              <div class="col-md-3">
                {{#if this.imagemUrl}}
                  <img src="{{this.imagemUrl}}" style="width:100%;max-width:220px;height:120px;object-fit:cover;border-radius:10px;border:1px solid #ddd;">
                {{else}}
                  <div style="width:220px;height:120px;border-radius:10px;border:1px dashed #bbb;display:flex;align-items:center;justify-content:center;color:#777;">
                    (sem imagem)
                  </div>
                {{/if}}
              </div>

              <div class="col-md-9">
                <div class="row g-2">
                  <div class="col-md-4">
                    <label class="form-label">Título</label>
                    <input class="form-control form-control-sm slotTitulo" value="{{this.titulo}}" data-id="{{this._id}}">
                  </div>
                  <div class="col-md-4">
                    <label class="form-label">Subtítulo</label>
                    <input class="form-control form-control-sm slotSubtitulo" value="{{this.subtitulo}}" data-id="{{this._id}}">
                  </div>
                  <div class="col-md-4">
                    <label class="form-label">Link</label>
                    <input class="form-control form-control-sm slotLink" value="{{this.link}}" data-id="{{this._id}}">
                  </div>
                </div>

                <div class="d-flex gap-2 align-items-center mt-3">
                  <input type="file" class="form-control form-control-sm slotFile" accept="image/*" data-id="{{this._id}}" style="max-width:360px;">
                  <button class="btn btn-sm btn-primary btnUpload" data-id="{{this._id}}" data-tipo="{{this.tipo}}">
                    Enviar foto (PC) e salvar
                  </button>

                  <button class="btn btn-sm btn-success btnSalvarTextos" data-id="{{this._id}}">
                    Salvar textos (título/link)
                  </button>
                </div>

                <div class="small text-muted mt-2">
                  URL atual: <span class="text-break">{{this.imagemUrl}}</span>
                </div>

              </div>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>

  <hr class="my-4">

  <h5>Novo slot</h5>
  <div class="row g-2">
    <div class="col-md-2">
      <select id="novoTipo" class="form-select form-select-sm">
        <option value="hero">hero</option>
        <option value="destaque">destaque</option>
        <option value="lateral">lateral</option>
      </select>
    </div>
    <div class="col-md-2">
      <input id="novoOrdem" class="form-control form-control-sm" placeholder="ordem" value="1">
    </div>
    <div class="col-md-4">
      <input id="novoTitulo" class="form-control form-control-sm" placeholder="título">
    </div>
    <div class="col-md-4">
      <button id="btnCriarSlot" class="btn btn-sm btn-outline-primary">Criar slot</button>
    </div>
  </div>

  <script src="/js/site/admin-home-layout.js"></script>
</body>
</html>
