<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Criar conta</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .card{ border-radius: 12px; }
    .form-control{ min-height: 42px; }
  </style>
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-12 col-sm-10 col-md-7 col-lg-5">
        <div class="card shadow-sm">
          <div class="card-body">
            <h4 class="mb-3 text-center">Criar conta</h4>

            {{#if erro}}<div class="alert alert-danger py-2">{{erro}}</div>{{/if}}

            <form method="post" action="/site/registrar" id="formReg">
              <div class="mb-3">
                <label class="form-label">Nome (opcional)</label>
                <input name="nome" class="form-control" maxlength="80" placeholder="Seu nome">
              </div>
              <div class="mb-3">
                <label class="form-label">E-mail</label>
                <input name="email" type="email" class="form-control" required placeholder="voce@email.com">
              </div>
              <div class="mb-3">
                <label class="form-label">Senha</label>
                <input name="senha" type="password" class="form-control" minlength="6" required>
              </div>
              <div class="mb-3">
                <label class="form-label">Confirme a senha</label>
                <input name="senha2" type="password" class="form-control" minlength="6" required>
                <div class="invalid-feedback">As senhas precisam ser iguais.</div>
              </div>
              <button class="btn btn-primary w-100">Criar conta</button>
            </form>

            <div class="text-center mt-3">
              <a class="small" href="/login">Já tenho conta</a>
            </div>
          </div>
        </div>
        <p class="text-center text-muted mt-3 small">
          Ao criar a conta, você concorda com os nossos termos de uso.
        </p>
      </div>
    </div>
  </div>

  <script>
  // Validação simples de confirmação de senha
  (function(){
    const f = document.getElementById('formReg');
    f?.addEventListener('submit', (ev)=>{
      const s1 = f.senha.value || '';
      const s2 = f.senha2.value || '';
      if (s1 !== s2){
        ev.preventDefault();
        f.senha2.classList.add('is-invalid');
        f.senha2.focus();
      }
    });
    f?.senha2?.addEventListener('input', ()=> f.senha2.classList.remove('is-invalid'));
  })();
  </script>
</body>
</html>
