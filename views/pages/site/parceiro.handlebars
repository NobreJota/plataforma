<style>
.parceiro-frame-wrapper {
  width: 100%;
  min-height: calc(100vh - 180px); /* ajusta conforme altura do topo */
  border: 1px solid #ddd;
  border-radius: 8px;
  overflow: hidden;
  background: #f8f9fa;
}

.parceiro-frame {
  width: 100%;
  height: calc(100vh - 200px);
  border: 0;
}

.parceiro-frame-wrapper{
  width: 100%;
  min-height: calc(100vh - 220px); /* ajuste fino se quiser */
  border: 1px solid #d9d9d9;
  border-radius: 10px;
  overflow: hidden;
  background: #f6f7f9;
}

.parceiro-frame{
  width: 100%;
  height: calc(100vh - 240px);     /* ajuste fino se quiser */
  border: 0;
  display: block;
}

.parceiro-logo{
  max-height: 90px;
  object-fit: contain;
}
</style>

<div class="container my-3">
  <div class="row g-3">
    <div class="col-12">
      <div class="d-flex align-items-center justify-content-between flex-wrap gap-2" style="background: palevioletred;">
        <div>
          <h1 class="h5 mb-1">Loja parceira · {{parceiro.nome}}</h1>
          {{#if parceiro.descricao}}
            <div class="text-muted small">{{parceiro.descricao}}</div>
          {{/if}}
        </div>

        <div class="d-flex gap-2">
          <a href="{{parceiro.url}}" target="_blank" rel="noopener"
             class="btn btn-sm btn-primary">
            Abrir em nova aba
          </a>
          <a href="/" class="btn btn-sm btn-outline-secondary">Voltar</a>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-3">
      <div class="card h-100">
        <div class="card-body text-center">
          {{#if parceiro.logo}}
            <img src="{{parceiro.logo}}" alt="{{parceiro.nome}}" class="img-fluid mb-2 parceiro-logo">
          {{/if}}

          <div class="small text-muted mb-2">
            Você está dentro do Urutal.
          </div>

          <div class="small mb-2">
            Plano: <strong>{{parceiro.plano}}</strong>
          </div>

          <a href="{{parceiro.url}}" target="_blank" rel="noopener"
             class="btn btn-sm btn-success w-100">
            Ir para o site
          </a>

          <hr>

          <div class="small text-muted">
            Se o site não carregar aqui, é bloqueio do próprio parceiro.
            Use “Abrir em nova aba”.
          </div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-9">
      <div class="parceiro-frame-wrapper">
        <iframe
          src="{{parceiro.url}}"
          class="parceiro-frame"
          loading="lazy"
          referrerpolicy="no-referrer">
        </iframe>
      </div>
    </div>
  </div>
</div>

